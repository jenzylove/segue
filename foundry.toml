[profile.default]
src = "src"
test = "test"
out = "out"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true
evm_version = "cancun"
threads = 1

[rpc_endpoints]
base = "${BASE_RPC_URL}"

[fmt]
line_length = 120
